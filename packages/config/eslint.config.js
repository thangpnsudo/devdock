// Minimal ESLint flat config placeholder.
// Phase 2 will populate with full config (typescript-eslint, react, hooks, etc).
/** @type {import('eslint').LintingConfig} */
export default [
  {
    rules: {
      'no-unused-vars': 'warn',
    },
  },
];
