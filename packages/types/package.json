{
  "name": "@devdock/types",
  "version": "0.1.0",
  "private": true,
  "description": "Shared TypeScript DTOs that mirror DevDock's Rust types",
  "license": "MIT",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "lint": "echo \"(no lint)\"",
    "format": "echo \"(no format)\"",
    "format:check": "echo \"(no format check)\"",
    "test": "echo \"(no tests)\"",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@devdock/config": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.6.2"
  }
}